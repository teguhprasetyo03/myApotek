<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yudha.myapotek">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/apotek"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/apotek_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateAct"></activity>
        <activity android:name=".LaporanAct" />
        <activity android:name=".SuccesBuy" />
        <activity android:name=".TambahObatAct" />
        <activity android:name=".KeranjangActivity" />
        <activity android:name=".DaftarObatAct" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".NewaccActivity" />
        <activity android:name=".SigninActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>